ixigo:
  postgres:
    host: dbpostgres
  dem-manager:
    dem-file-props:
      root-folder: "/demFiles"
      parser-environment: "RASP"             # Enum: WINDOWS, RASP
      parser-execution-type: ASYNC                                                                                           # Enum: ASYNC, SYNC
      parser-exec-path: '/demParser/DemParser'       # This is the full path to the DemParser executable
      
      